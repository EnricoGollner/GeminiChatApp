name: chat_bot_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_gemini: ^2.0.0
  image_picker:
  flutter_dotenv: ^5.1.0
  provider: ^6.1.1
  hive_flutter: ^1.1.0
  hive:
  lottie: ^2.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  hive_generator:
  build_runner:

flutter:
  uses-material-design: true

  assets:
    - .env
    - assets/